# DiCode 上下文压缩测评报告：修改后 V2 与 Legacy 对比

日期：2026-08-02  
状态：基于现有证据的阶段性正式报告

## 1. 执行摘要

修改后的 V2 在当前测评范围内优于 Legacy 的主要方面不是单一 benchmark 分数，而是：用更少的上下文 token，保留更结构化、更适合继续执行的任务状态，同时没有在三条高区分度真实续作案例中出现 V2 特有的错误方向。

在 6 条 byte-identical 真实历史的直接对照中：

| 指标                     | 修改后 V2 |  Legacy |                              对比 |
| ------------------------ | --------: | ------: | --------------------------------: |
| 输入 normalized tokens   |   295,830 | 295,830 |                          完全相同 |
| 压缩后 normalized tokens |    25,562 |  37,561 | V2 少 11,999，较 Legacy 少 31.95% |
| 保留率                   |     8.64% |  12.70% |           V2 更紧凑 4.06 个百分点 |
| 释放率                   |    91.36% |  87.30% |           V2 多释放 4.06 个百分点 |
| 九章节结构化摘要         |       6/6 |     0/6 |                       V2 全部满足 |
| 不同读取目标路径逐字保留 |     62/64 |   62/64 |                              相同 |
| 执行命令逐字保留         |     46/85 |   10/85 |                       V2 明显更多 |

在 10 条另一批高风险真实历史的直接产物审计中，修改后 V2 对执行命令的逐字保留为 `64/145`，Legacy 为 `14/145`。V2 有 12 个未逐字保留的读取目标，经源历史和工具结果逐项复核后，没有一项满足“遗漏任务关键事实并导致错误续作”的缺陷标准。

在 Ansible、Django、LangChain 三条真实历史上，对 Full、V2、Legacy、Naive 四路共生成 12 个匿名续作计划：`12/12` 完成并通过 schema 与 SHA 锁校验。V2 的 3 个计划均未选错首要实现文件，未遗漏关键分支，未把失败命令记成成功，也未把推测伪装成事实。Legacy 的 3 个计划同样可用，但在 Django 案例中对具体实现条件比源历史支持的证据更武断。

因此，本报告支持以下结论：

> 修改后 V2 已达到“可作为生产主路径、Legacy 作为运行时回滚路径”的证据水平；但现有样本不足以授权删除 Legacy，也不能把续作计划测评表述为完整任务成功率。

## 2. 测评目标与原则

本次测评的目标是发现并修复 DiCode 上下文压缩的生产级瑕疵，而不是优化排行榜分数。

比较遵循以下约束：

1. V2 与 Legacy 接收 byte-identical canonical history。
2. 官方 `problem_statement` 对所有路由可见；gold context、reference patch、test patch 和评分结果不进入压缩输入。
3. 直接产物保真与真实续作可执行性是主要缺陷证据。
4. ContextBench reader 和官方 harness 分数用于定位风险，不把单次 reader 波动直接归因于压缩算法。
5. 测评 adapter、reader 重试和 harness 修复不计作 V2 产品质量提升。
6. Legacy 始终作为兼容与回滚基线保留。

## 3. 被测对象

### 3.1 修改后 V2

本报告中的“修改后 V2”包含本阶段已经验证的上下文压缩修复：

- 全局自动压缩阈值不再被历史 Profile `50%` 阈值暗中覆盖；
- 摘要后重新计量，仍超硬预算时继续确定性减量，不能误报成功；
- protected continuation 使用 token 感知预算，按 15% 上限保存必要工具状态；
- 重复失败结果去重，超长失败和项目工具证据有界保留；
- 完整九字段但缺少 `version: 1` 的结构化结果可安全识别，残缺或未知版本仍拒绝；
- Files 章节保留源历史已经知道的相对路径、符号和行号/范围；
- provider overflow 从实际失败的 request projection 继续恢复；
- 大工具输出和附件执行生产级准入与安全降级。

### 3.2 Legacy

Legacy 是现有兼容压缩路径，也是生产回滚依赖。它更倾向保留时间顺序的历史复述和探索轨迹，但没有 V2 的固定九章节续作状态契约。

本次没有修改 Legacy 算法、旧 parent-ID 语义、兼容 reader 或持久化合同。

## 4. 数据集与证据分层

| 证据层             |                    样本 | 用途                                            |
| ------------------ | ----------------------: | ----------------------------------------------- |
| 单例修复闭环       |      1 条，86,837 token | 证明预算、protected continuation 和结构解析修复 |
| 高风险直接产物审计 |                   10 条 | 对照修改后 V2、Legacy 和源历史的事实保留        |
| 补充直接产物审计   |                    6 条 | 跨目录、源码—测试、配置、负面搜索和多格式证据   |
| 匿名真实续作       | 3 条 × 4 路 = 12 个计划 | 检查下一步文件、分支、命令、事实边界和重复探索  |
| 位置保留泛化       |                   10 条 | 验证已知符号/行号不再系统性退化为路径-only      |
| 历史官方横向基线   |            10 条 × 4 路 | 发现修复前定位精度缺陷；不能冒充修改后总分      |

主要证据位于：

`E:\DiCodeContextBench\runs\contextbench-v2-defect-discovery-v1`

## 5. 修改后 V2 与 Legacy 的压缩效率

### 5.1 六条同输入直接对照

| 案例                   |  输入 token |  V2 压缩后 | Legacy 压缩后 | V2 相对 Legacy |
| ---------------------- | ----------: | ---------: | ------------: | -------------: |
| qutebrowser `64fffdfa` |      34,538 |      4,409 |         6,068 |      少 27.34% |
| Ansible `84fdadf0`     |      66,629 |      5,334 |         8,805 |      少 39.42% |
| qutebrowser `942d0b14` |      51,165 |      5,009 |         5,525 |       少 9.34% |
| qutebrowser `c1f435de` |      53,468 |      4,910 |         6,744 |      少 27.19% |
| Django `497deb6f`      |      30,858 |      1,886 |         5,037 |      少 62.56% |
| LangChain `9e2901b5`   |      59,172 |      4,014 |         5,382 |      少 25.42% |
| **合计**               | **295,830** | **25,562** |    **37,561** |  **少 31.95%** |

V2 在 6/6 案例中都比 Legacy 更小。更小本身不代表更好，因此必须结合下面的事实保留和续作质量判断。

### 5.2 单例修复闭环

Ansible `18d7bbbc` 的 canonical history 为 86,837 normalized tokens：

| 路由      | 压缩后 token | 结构状态                                                |
| --------- | -----------: | ------------------------------------------------------- |
| 修复前 V2 |        8,879 | protected continuation 过大；修复过程中还复现过滑窗降级 |
| Legacy    |        8,084 | 非九章节摘要                                            |
| 修改后 V2 |        5,064 | 1 个完整九章节摘要，无截断标记                          |

修改后 V2 比 Legacy 少 3,020 token，即少 37.36%。protected continuation 使用 1,226/1,228 token，既保留 6 个独立工具状态，又恢复硬预算。

## 6. 任务关键信息保留

### 6.1 六条补充批次

- V2 和 Legacy 都逐字保留 `62/64` 个不同读取目标路径。
- V2 逐字保留 `46/85` 条执行命令，Legacy 为 `10/85`。
- V2 `6/6` 产生一个完整九章节摘要，输出范围为 1,886–5,334 token。
- 两个 V2 未逐字保留的路径分别是 Ansible 兼容导入 shim 和 LangChain 通用 Runnable 探索范围；二者都没有提供当前修复所需的新事实。
- 对 Objective、Pending Work、Files、Failures、Decisions 做直接人工复核，没有发现 V2 与源历史冲突、错误续作方向或关键证据来源丢失。

### 6.2 十条高风险批次

- V2 执行命令逐字保留 `64/145`，Legacy 为 `14/145`。
- V2 的 10 条产物都保持九章节结构、预算内输出和可执行续作状态。
- 机械索引发现 V2 未逐字保留 12 个读取目标，涉及 9 个不同路径。
- 逐项追踪后，它们属于空壳文件、错误方向、旧故障落点、通用基类，或已被摘要吸收为结论的支撑证据。
- Ansible 正常 YAML 列表 fixture 和 SAM low-resolution mask 来源属于“可接受但继续观察”：V2 保存了结论、实现点和测试点，但没有保存支撑文件路径。

这说明 Legacy 更接近历史复述，V2 更接近续作状态压缩。不能以“复述过的路径更多”直接判定 Legacy 更好；也不能因为 V2 更结构化就忽略其潜在证据来源丢失风险。

## 7. 精确位置缺陷与修复效果

历史 10 条高风险官方对照暴露了修改前 V2 的真实缺陷：文件覆盖率不差，但符号、span 和 line precision 低于 Legacy。其历史平均值如下：

| 历史指标（修复前 V2） |     V2 | Legacy |
| --------------------- | -----: | -----: |
| file coverage         | 59.03% | 57.03% |
| file precision        | 59.08% | 68.05% |
| symbol coverage       | 58.87% | 60.88% |
| symbol precision      | 29.05% | 31.34% |
| span coverage         | 51.34% | 58.65% |
| span precision        | 24.94% | 31.56% |
| line coverage         | 50.86% | 57.77% |
| line precision        | 15.57% | 24.98% |

该结果不能作为修改后 V2 的总分，但它成功定位了“已知位置退化为路径-only”的生产缺陷。

修复后，10/10 高风险历史都增加了由源历史支持的符号或行位置证据：

- 首批 3 条：`0/1/1` 提升为 `19/7/10`；
- 泛化 7 条：`0/2/7/3/0/0/1` 提升为 `8/6/14/18/46/8/11`；
- 10/10 保持九章节结构并落入预算。

原 reader 在摘要已有精确位置时仍可能输出整文件宽范围，因此 reader 契约随后被收紧。由于 reader 契约发生变化，旧官方四路分数不能与新结果直接混算；要获得严格的“修改后官方总分”，必须让 Full、V2、Legacy、Naive 使用同一新版 reader 全量重跑。

## 8. 真实续作对比

### 8.1 总体结果

- Ansible、Django、LangChain 三条案例；
- Full、V2、Legacy、Naive 四路，共 12 个匿名计划；
- `12/12` 完成；
- `12/12` 通过严格 schema、case/input/candidate SHA 锁；
- 无 Provider 错误、空结果、JSON 解析失败或部分终态。

### 8.2 V2 与 Legacy 逐案判断

| 案例                  | 修改后 V2                                                                                                          | Legacy                                                                    | 判断                                           |
| --------------------- | ------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------- | ---------------------------------------------- |
| Ansible no_log        | 保留 Python/C# 差异、测试位置、失败的 bare `pytest`、未确认 helper 名称及最小验证路径                              | 同样给出可执行计划，但实现位置和签名存在推测                              | 两者可用；V2 的事实/不确定性分界更清晰         |
| Django RadioSelect    | 正确限制在通用 ModelForm 路径，保留 required/optional、Select、`blank=True` 和 406-test baseline；对插入点保持保守 | 直接建议具体 `ModelChoiceField.__init__` 条件，但该细节未被源历史完全确认 | V2 更稳健；Legacy 更武断但未证明会导致错误补丁 |
| LangChain local cache | 保留 sync/async、local/global cache、参考实现、测试矩阵和 pytest/poetry 失败                                       | 同样可恢复任务方向                                                        | 两者可用；未发现 V2 特有遗漏                   |

本轮 V2 没有出现：

- 选错首要实现文件；
- 遗漏同步/异步、required/optional 等关键分支；
- 把失败命令误记为成功；
- 把猜测写成确认事实；
- 无意义地重复完整探索；
- 丢失修改后的最小验证路径。

## 9. 已发现并闭环的生产缺陷

测评并非只证明“通过”，而是发现并推动修复了以下缺陷：

1. Profile 历史阈值可能覆盖 UI 可见的全局阈值。
2. 摘要成功后仍可能超硬输入预算并被误报成功。
3. protected continuation 使用字符预算，可能挤占整个摘要。
4. 10% 保护配额无法容纳多个独立工具状态，会退化到滑窗截断。
5. 状态键重复携带原始工具输入，造成不必要负担。
6. DeepSeek 返回完整九字段但缺少 `version` 时被错误降级为自由文本。
7. 已知函数名和行号被摘要退化成路径-only，导致宽范围重复读取。
8. provider overflow 恢复可能返回 canonical history 重复错误投影。
9. 大工具输出和附件缺少统一生产准入与可操作的安全降级。

这些缺陷均已进入聚焦回归或确定性门禁；没有通过修改评分器来掩盖产品问题。

## 10. 不能从本报告推出的结论

本报告不能证明：

- V2 在所有仓库、语言、模型和超长会话上绝对优于 Legacy；
- 3 个续作计划等同于 3 个完整补丁通过官方测试；
- 修复后 V2 已有可与旧数据直接拼接的官方 ContextBench 总分；
- 本机能够完成固定远端后端的发布环境联调；
- 可以删除 Legacy、迁移持久化记录或清理旧 parent-ID 语义。

尤其需要注意：本次续作测评评估的是“下一步是否正确、可执行、证据边界是否可靠”，不是最终代码补丁正确率。

## 11. 生产判断与回滚策略

### 11.1 当前判断

修改后 V2 可以作为受控生产候选的默认主路径，理由是：

- 同输入下比 Legacy 少 31.95% 压缩后 token；
- 6/6 结构化摘要完整；
- 关键路径召回不低于 Legacy，命令逐字保留显著更多；
- 10/10 位置证据修复泛化成立；
- 3/3 V2 真实续作没有发现 V2 特有错误方向；
- 已知失败终态和预算恢复均有确定性回归。

### 11.2 Legacy 必须继续保留

出现下列任一情况，应切回 Legacy 并保存 V2 证据：

- V2 连续失败、取消或无法恢复硬预算；
- provider overflow 有界恢复没有进展；
- 摘要结构无效或丢失关键约束/精确位置并导致错误续作；
- protected continuation 或附件无法安全表示；
- 发布环境出现冻结接口兼容问题。

回滚不得删除 canonical history，也不得触发持久化迁移。

## 12. 后续测评建议

1. 使用同一最新版 route-neutral reader 对 Full、修改后 V2、Legacy、Naive 做一次全路由重跑，形成严格可比较的修改后官方指标。
2. 增加“关键设计结论来自非修改文件”的定向样本，继续观察 V2 是否同时保留结论和证据来源路径。
3. 增加完整 Agent 任务层回归，但将补丁质量、Agent 行为、工具环境和压缩质量分别归因。
4. 在可访问真实远端后端的发布环境验证一次自动阈值触发、成功压缩、overflow 恢复和 Legacy 回滚。
5. 在上述证据稳定前，不启动 Milestone 10 或 Legacy 清理。

## 13. 证据索引

- 生产候选总览：`docs/context-compaction/V2_PRODUCTION_CANDIDATE_REPORT_2026-08-01.md`
- 测评管线：`docs/context-compaction/V2_CONTEXTBENCH_EVALUATION_PIPELINE.md`
- 生产危险情况：`docs/context-compaction/上下文危险情况.md`
- 单例修复闭环：`E:\DiCodeContextBench\runs\contextbench-v2-defect-discovery-v1\compression-audit-postfix-v2`
- 10 条高风险批次：`E:\DiCodeContextBench\runs\contextbench-v2-defect-discovery-v1\high-risk-batch-v1`
- 6 条补充产物与 3 条续作：`E:\DiCodeContextBench\runs\contextbench-v2-defect-discovery-v1\evidence-source-batch-v1`
- 历史官方分析：`high-risk-batch-v1\official-analysis.json`
- 位置修复对照：`high-risk-batch-v1\location-preservation-postfix-v1\location-preservation-comparison.json`
- 位置泛化：`high-risk-batch-v1\location-preservation-generalization-v1\location-generalization-report.json`
- 匿名续作原始结果：`evidence-source-batch-v1\continuation-readiness.jsonl`

## 14. 最终结论

在现有可复核证据下，修改后 V2 相比 Legacy 实现了更强的压缩、更稳定的结构化续作状态和更好的命令/验证上下文保留，没有在已测高风险案例中引入 V2 特有的错误续作。Legacy 仍具有更忠实复述历史路径的特点，并且是必要的生产回滚保障。

因此推荐：**V2 作为受控生产主路径，Legacy 保留为运行时回滚路径；继续补齐同 reader 官方横向重跑和真实后端发布验收，不删除 Legacy。**
